export enum eWorldRole {
  ADMIN,
  TRUSTED,
  USER,
  PUBLIC,
}

export enum eObjectPermission {
  WRITE,
  READ,
  NONE,
}

export enum eObjectTypes {
  Document = "Document",
  DocumentTemplate = "DocumentTemplate",
  Folder = "Folder",
  Section = "DocumentSection",
}

export enum eSectionTypes {
  Text = "Text",
}
